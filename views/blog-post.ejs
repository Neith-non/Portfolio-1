<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internship Journey | Neithan Deniel B. Gula</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;600;700&family=Syne:wght@400;700;800&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        brand: {
                            lightest: '#eaebfe', 
                            light: '#b0b8ce',   
                            mid: '#505a74',      
                            dark: '#354c7c',     
                            deep: '#022954',     
                        }
                    },
                    fontFamily: {
                        sans: ['"Space Grotesk"', 'sans-serif'],
                        display: ['"Syne"', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-brand-deep text-brand-lightest font-sans min-h-screen relative overflow-x-hidden selection:bg-brand-lightest selection:text-brand-deep">

    <div class="fixed top-0 left-0 w-full h-full bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-20 pointer-events-none z-0"></div>
    
    <div class="fixed -top-32 -right-32 w-[600px] h-[600px] bg-emerald-500/10 rounded-full blur-[120px] pointer-events-none z-0 animate-pulse"></div>
    
    <div class="fixed -bottom-40 -left-40 w-[800px] h-[800px] bg-brand-lightest/5 rounded-full blur-[150px] pointer-events-none z-0"></div>

    <div class="relative z-10 max-w-7xl mx-auto px-6 py-12 lg:px-12 lg:py-20">
        
        <a href="/updates" class="inline-flex items-center gap-2 text-brand-light hover:text-brand-lightest transition-colors mb-16 uppercase text-xs font-bold tracking-widest group bg-brand-dark/30 px-4 py-2 rounded-full backdrop-blur-md border border-brand-mid/30 hover:border-brand-mid">
            <span class="group-hover:-translate-x-1 transition-transform">←</span> Back to Timeline
        </a>

        <header class="mb-16 border-b border-brand-mid/30 pb-12 relative">
            <div class="flex flex-wrap gap-3 mb-8">
                <span class="text-brand-deep bg-emerald-400 text-xs font-bold px-3 py-1 uppercase tracking-widest rounded shadow-[0_0_15px_rgba(52,211,153,0.4)]">Log: Internship</span>
                <span class="text-brand-light text-xs font-mono py-1 border border-brand-mid/50 px-3 rounded bg-brand-dark/20 backdrop-blur-sm">Year 2026</span>
            </div>
            
            <h1 class="font-display font-bold text-5xl lg:text-7xl text-transparent bg-clip-text bg-gradient-to-r from-brand-lightest to-brand-light leading-[1.1] mb-8 max-w-5xl drop-shadow-lg">
                <%= data.title %>
            </h1>
            
            <p class="text-brand-light text-xl leading-relaxed max-w-4xl border-l-4 border-brand-dark pl-6">
                <%= data.intro %>
            </p>
        </header>

        <article class="space-y-24 max-w-6xl">
            
            <% data.logs.forEach(log => { %>
                
                <% 
                    // UPDATED: Now includes random placements on ALL sides (top, bottom, left, right)
                    const positions = [
                        "-top-6 -right-2 md:-top-8 md:-right-8 rotate-6 md:rotate-12", // Top Right
                        "-bottom-4 -right-2 md:-bottom-8 md:-right-6 -rotate-6 md:-rotate-12", // Bottom Right
                        "top-1/3 -right-3 md:-right-12 rotate-6 md:rotate-12", // Middle Right
                        "-top-6 -left-2 md:-top-8 md:-left-8 -rotate-6 md:-rotate-12", // Top Left
                        "-bottom-4 -left-2 md:-bottom-8 md:-left-8 rotate-6 md:rotate-12", // Bottom Left
                        "top-1/3 -left-3 md:-left-12 -rotate-3 md:-rotate-6", // Middle Left
                        "-top-8 left-1/2 -translate-x-1/2 rotate-3 md:rotate-6", // Top Center
                        "-bottom-8 left-1/3 -translate-x-1/2 -rotate-6 md:-rotate-12" // Bottom Center slightly off
                    ];
                    // Pick a random position from the array
                    const randomPos = positions[Math.floor(Math.random() * positions.length)];
                %>

                <section class="relative pl-8 md:pl-16 border-l-[3px] border-brand-dark group">
                    
                    <div class="absolute -left-[11px] top-10 w-5 h-5 bg-brand-deep border-[4px] border-emerald-400 rounded-full shadow-[0_0_15px_rgba(52,211,153,0.6)] group-hover:scale-125 transition-transform duration-300"></div>
                    
                    <div class="relative w-full group/card">

                        <% if (log.gif) { %>
                            <div class="absolute z-30 <%= randomPos %> w-16 h-16 md:w-20 md:h-20 drop-shadow-2xl pointer-events-none group-hover/card:scale-125 transition-transform duration-500">
                                <img src="<%= log.gif %>" alt="Funny Gif" class="w-full h-full object-contain filter drop-shadow-[0_0_8px_rgba(234,235,254,0.3)]">
                            </div>
                        <% } %>

                        <div class="relative bg-brand-dark/20 backdrop-blur-xl border border-brand-mid/20 group-hover/card:border-emerald-400/40 rounded-2xl p-8 md:p-12 shadow-2xl transition-all duration-500 overflow-hidden">
                            
                            <div class="absolute -top-20 -right-20 w-64 h-64 bg-brand-lightest/0 rounded-full blur-[80px] group-hover/card:bg-emerald-400/10 transition-colors duration-700 pointer-events-none"></div>

                            <div class="mb-8 relative z-10">
                                <div class="flex flex-col md:flex-row md:items-baseline gap-2 md:gap-6 mb-3">
                                    <h2 class="text-4xl font-display font-bold text-brand-lightest uppercase tracking-wide drop-shadow-md"><%= log.week %></h2>
                                    <span class="text-brand-mid font-mono text-sm bg-brand-deep/50 px-3 py-1 rounded border border-brand-mid/20"><%= log.date %></span>
                                </div>
                                <h3 class="text-2xl font-bold text-emerald-400"><%= log.title %></h3>
                            </div>

                            <div class="space-y-6 text-brand-lightest/90 text-lg leading-relaxed mb-10 relative z-10 max-w-4xl">
                                <% log.content.forEach(paragraph => { %>
                                    <p><%= paragraph %></p>
                                <% }) %>
                            </div>

                            <% if (log.images && log.images.length > 0) { %>
                                <div class="mt-8 pt-8 border-t border-brand-mid/20 relative z-10">
                                    <h4 class="text-xs uppercase tracking-widest text-brand-mid mb-6 font-bold">Attached Media</h4>
                                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                                        <% log.images.forEach(imgSrc => { %>
                                            <div class="relative group/img overflow-hidden rounded-xl border border-brand-mid/30 shadow-lg bg-brand-deep">
                                                <div class="absolute inset-0 bg-brand-deep/20 group-hover/img:bg-transparent transition-colors duration-500 z-10 pointer-events-none"></div>
                                                <img src="<%= imgSrc %>" alt="Log Photo" class="w-full h-auto object-cover aspect-video group-hover/img:scale-110 transition-transform duration-700">
                                            </div>
                                        <% }) %>
                                    </div>
                                </div>
                            <% } %>

                        </div>
                    </div>
                </section>
            <% }) %>

        </article>

        <div class="mt-32 pt-10 border-t border-brand-mid/20 flex flex-col md:flex-row justify-between items-center gap-4 text-sm font-mono text-brand-mid">
            <div class="flex items-center gap-3">
                <div class="w-8 h-8 rounded-full overflow-hidden border border-brand-mid">
                    <img src="/images/me.jpg" alt="Author" class="w-full h-full object-cover">
                </div>
                <span>Author: Neithan D. Gula</span>
            </div>
            <span class="tracking-widest uppercase text-xs">End of transmission.</span>
        </div>

    </div>

</body>
</html>